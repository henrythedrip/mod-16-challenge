<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="./assets/style.css">
    <title>{{pageTitle}}</title>
    <style>
        
    </style>
</head>
<body>

    <header>
        <h1>{{siteTitle}} 💧 Merch & Music</h1>
    </header>

    <nav>
        {{#each navItems}}
            <a href="{{url}}">{{label}}</a>
        {{/each}}
    </nav>

    <section>
        {{#each products}}
            <div class="product">
                <p>{{productDescription}}: <a href="{{productLink}}" target="_blank">{{productName}}</a></p>
            </div>
        {{/each}}
    </section>

    <footer>
        <p>&copy; {{currentYear}} {{siteTitle}} 💧 Merch. All rights reserved.</p>
    </footer>

</body>
</html>
