 <section>
    {{#each products}}
      <div class="product">
            <img src={{img}} alt="{{altText}}">
            <h3>{{productName}}</h3>
            <p>{{productDescription}}</p>
            <p>${{price}}</p>
            <button>Buy Now</button>
        </div>

    {{/each}}
 </section>